# TumorNet 2000 — real pipeline
#
# Install:  python3 -m pip install -r requirements.txt
#
# torch>=2.2 is the floor for usable MPS support on Apple silicon. On an M2 Max
# the default wheel from PyPI already ships the MPS backend — no extra index URL.

numpy>=1.26
nibabel>=5.0
scipy>=1.10
scikit-image>=0.21
scikit-learn>=1.3
matplotlib>=3.7
Pillow>=10.0
torch>=2.2
